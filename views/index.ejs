<h1>MY EXPRESS SERVER IS WORKING!</h1>

<hr>

<h2>PostgreSQL Database Status:</h2>

<% if (dbStatus === 'Success') { %>
    <p>✅ **CONNECTION SUCCESSFUL!**</p>
    <p>Version: <%= dbVersion %></p>
<% } else { %>
    <p>❌ **CONNECTION FAILED:**</p>
    <p>Error: <%= dbVersion %></p>
<% } %>

<hr>

<h2>Database Schema (Tables) Status:</h2>

<% if (tablesStatus === 'Ready') { %>
    <p>✅ **ALL 9 TABLES CREATED/READY!**</p>
    <p>The core schema for 'SkillSwap' is in place.</p>
<% } else { %>
    <p>❌ **TABLE CREATION FAILED!**</p>
    <p>Please check the server logs (index.js) for details on which DDL failed.</p>
<% } %>